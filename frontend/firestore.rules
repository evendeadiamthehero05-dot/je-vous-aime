rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated users can access rooms
    match /rooms/{roomId} {
      // Allow read if user is authenticated and is a participant in the room
      allow read: if request.auth != null && 
                     request.auth.uid in resource.data.participants;
      
      // Allow write if user is authenticated and is a participant in the room
      allow write: if request.auth != null && 
                      request.auth.uid in resource.data.participants;
      
      // Allow creation (initial write) by the room creator during creation flow
      // Note: This is restricted and should only be called from backend
      allow create: if request.auth != null;
    }
  }
}